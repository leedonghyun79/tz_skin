# 보안상 깃헙에 올리면 안 되는 파일들 (Security)
.env
.env.*
*.pem
*.key
config/database*
config/secrets*

# 불필요한 임시 파일 및 캐시 (Cache & Temp)
.temp/
.tmp/
*.log
*.bak
*.swp
*.zip
*.tar.gz

# 운영체제 자동 생성 파일 (OS)
.DS_Store
Thumbs.db
Desktop.ini

# 에디터 및 IDE 설정 파일 (Editor)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 패키지 의존성 (추후 npm 등을 사용할 경우)
node_modules/
dist/
build/

# Local crawler data
_local_crawler_data/
itemexcel.xls

